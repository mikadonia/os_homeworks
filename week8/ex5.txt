Virtual addresses form virtual address space that consists of pages (some equal fixed size units) and when VM is used Virtual addresses go directly to memory management unit. MMU maps this addresses to the physical addresses. Virtual address is generated by CPU
Physical addresses form page frames (equal size units in the physical memory). Physical address is actually available on memory unit (location in RAM)
